name: 🧪 Manual E2E Tests

on:
  workflow_dispatch:
    inputs:
      pattern:
        description: 'Test file or grep pattern (e.g. "e2e/home.spec.ts" or "search")'
        required: true
        type: string
      browser:
        description: 'Browser'
        required: false
        default: 'chromium'
        type: choice
        options: [chromium, firefox, webkit, all]
      retries:
        description: 'Retry count'
        required: false
        default: '2'
        type: string
      base_url:
        description: 'Base URL to test against (default: local build). Use https://devprep-pages.pages.dev for production.'
        required: false
        default: ''
        type: string

permissions:
  contents: read

jobs:
  test:
    name: 🧪 Run E2E Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-node-pnpm

      - name: Validate inputs
        run: |
          PATTERN='${{ inputs.pattern }}'
          if echo "$PATTERN" | grep -qE '[`$\\]'; then
            echo "❌ Pattern contains unsafe characters"
            exit 1
          fi
          if [[ ! "${{ inputs.retries }}" =~ ^[0-9]+$ ]]; then
            echo "❌ retries must be a non-negative integer"
            exit 1
          fi
          echo "✅ Inputs validated"

      - name: Fetch test data
        run: node script/fetch-questions-for-build.js
        continue-on-error: true

      - name: Install Playwright browsers
        run: |
          if [ "${{ inputs.browser }}" = "all" ]; then
            pnpm exec playwright install --with-deps
          else
            pnpm exec playwright install --with-deps ${{ inputs.browser }}
          fi

      - name: Build application
        if: ${{ inputs.base_url == '' }}
        run: pnpm run build && pnpm run build:pagefind

      - name: Run tests (local build)
        if: ${{ inputs.base_url == '' }}
        run: |
          BROWSER_FLAG=""
          [ "${{ inputs.browser }}" != "all" ] && BROWSER_FLAG="--project=${{ inputs.browser }}-desktop"

          PATTERN="${{ inputs.pattern }}"
          if [[ "$PATTERN" == *".spec.ts"* ]] || [[ "$PATTERN" == *"e2e/"* ]]; then
            pnpm exec playwright test "$PATTERN" $BROWSER_FLAG \
              --retries=${{ inputs.retries }}
          else
            pnpm exec playwright test --grep "$PATTERN" $BROWSER_FLAG \
              --retries=${{ inputs.retries }}
          fi
        env:
          CI: 'true'
          SERVE_STATIC: 'true'
          PLAYWRIGHT_BASE_URL: 'http://localhost:5001'

      - name: Run tests (remote URL)
        if: ${{ inputs.base_url != '' }}
        run: |
          BROWSER_FLAG=""
          [ "${{ inputs.browser }}" != "all" ] && BROWSER_FLAG="--project=${{ inputs.browser }}-desktop"

          PATTERN="${{ inputs.pattern }}"
          if [[ "$PATTERN" == *".spec.ts"* ]] || [[ "$PATTERN" == *"e2e/"* ]]; then
            pnpm exec playwright test "$PATTERN" $BROWSER_FLAG \
              --retries=${{ inputs.retries }}
          else
            pnpm exec playwright test --grep "$PATTERN" $BROWSER_FLAG \
              --retries=${{ inputs.retries }}
          fi
        env:
          CI: 'true'
          PLAYWRIGHT_BASE_URL: '${{ inputs.base_url }}'

      - name: Compress & upload results
        if: always()
        run: tar -czf test-results.tar.gz test-results/ playwright-report/ 2>/dev/null || true

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: e2e-results-${{ github.run_number }}
          path: test-results.tar.gz
          retention-days: 7

      - name: Summary
        if: always()
        run: |
          TARGET="${{ inputs.base_url != '' && inputs.base_url || 'local build' }}"
          {
            echo "## 🧪 E2E Test Results"
            echo ""
            echo "**Pattern:** \`${{ inputs.pattern }}\`"
            echo "**Browser:** ${{ inputs.browser }}"
            echo "**Retries:** ${{ inputs.retries }}"
            echo "**Target:** $TARGET"
          } >> "$GITHUB_STEP_SUMMARY"
