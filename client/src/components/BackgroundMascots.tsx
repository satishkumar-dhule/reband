/**
 * Background Mascots - Disabled
 * Mascots are disabled in the clean theme system
 */

import { useLocation } from 'wouter';

export default function BackgroundMascots() {
  const [location] = useLocation();
  return null;
}
